# summarize — improvement log

## 2026-09-07 出力が「壁」になり、識別子だらけで密度が低い

- **実物**: セッション 6329b45a (ma-splitflap) で `/summarize` を2回実行。いずれも
  見出し0・箇条書き0・1段落400字超・コード参照13個の散文の塊。ユーザーの指摘:
  「適切に見出しやリストを使い、読者の負荷を減らして」「基本的には意味論で記述し
  不要なコード参照は抑制し情報密度をあげて欲しい」。
  実験素材と全出力: `evals/2026-09-07-structure/`

- **分類**: 内容欠陥（主）。エージェントはスキルに忠実に従っており、遵守・理解の
  失敗ではない。従った結果が悪かった。副次的に暗黙判断欠陥（"short paragraphs"
  に基準がなく400字でも "short" と自己判定できる）。

- **5-why の終点**: スキルが「散文 vs 箇条書き」「識別子 vs 曖昧(vibes)」という
  二値対立で書かれており、実際に必要な中間を表現できていなかった。
  - 見出し禁止の根拠は「20項目の変更ログ」への対抗だったが、必要だったのは
    「2〜4個の意味ブロックへの分割」。両者を同一視して一括禁止していた。
  - "Anchor claims to concrete artifacts — file paths, function names, commit
    hashes" が無条件指示で、vibes の対義語を「識別子の列挙」と規定していた。
  - 英語圏前提: 日本語は語間空白がなく、同語数でも塊の走査コストが高い。

- **修正**:
  - 「2–4 short paragraphs in prose」→「2–4 labelled sections」。見出しは
    *意味* を名指すこと("概要"のような無内容ラベルを明示的に禁止)。
  - 1段落200字(150語)の**観測可能な上限**を追加（暗黙判断欠陥への対処）。
  - リストは "peer items のときだけ" という条件文に（無条件禁止をやめた）。
  - 「Write in meaning; anchor sparingly」節を新設。識別子は「読者が行動/検証に
    使うとき」だけ、数値は例外として保持、と条件化。
  - 矛盾する2行を削除（"Don't produce a wall of headers" / "gets the full 2–4"）
    および description の "a few-paragraph narrative" を修正。矛盾を残すと
    静かに片方が選ばれる。
  - 例文を英語のETL例から、実際に起きた失敗（意味論で書けている版）に差し替え。
  - **正味 54行 → 53行**。足した分だけ引いた。

- **実験**: 新旧文面を新鮮なサブエージェントに与え、同一素材で各5回。
  A(現行) 5/5 が見出し0・最長段落430–470字・250字超が3–4個・コード参照6–7個。
  B(候補) 5/5 が見出し3–5個・最長段落200字・250字超0個・コード参照0–3個。
  分散も小さく、両アームとも一貫して収束（＝A は確実に壁を生産していた）。
  B は識別子を意味に言い換えつつ、決定的な数値(848→170 / 96発/100ms)は5/5で保持。
  詳細: `evals/2026-09-07-structure/results.md`

- **回帰**: (1) 3メッセージの極小セッション → 2ブロックのみ。過剰構造にならず
  「次にやること: 特になし」と正しく明示。"Match the weight" は生存。
  (2) 英語セッション → 英語で出力、意味を名指す見出し。`SELECT *` や列名は
  「読者が行動/検証に使う」ため保持されており、抑制が効きすぎてもいない。
  ただし最終節だけ「次にやること」と日本語ラベルが混入 → 改訂で
  「書いている言語の見出しにする」と明示し、再実行で解消を確認。
