| title | level | Status | Priority | Size |
|---|---|---|---|---|
| fork で作ったセッションが Navigator の一覧で親の下に見える | Story | Backlog | P1 | (blank) |
| feature/fork-detection をレビュー指摘を解消して main に取り込む | Task | Backlog | (blank) | (blank) |
| 親未登録の fork 行のタイトルも _fork_label 経由で生成する | Task | Backlog | (blank) | (blank) |
| nearest-parent と compaction 非 fork の設計保証をテストで固定する | Task | Backlog | (blank) | (blank) |
| Navigator 起動時に端末を失った古い navigator プロセスを掃除する | Task | Backlog | P2 | (blank) |
| navigator-list.sh を責務ごとに 500 行以下に分ける | Task | Backlog | P2 | (blank) |

Priority は Story にのみ設定するルールに従い、fork 表示バグの Story は既存機能の可視な不具合として P1、親を持たない独立 Task 2件はクリーンアップ・技術的負債として実害の緊急度が低いため P2 とした。
