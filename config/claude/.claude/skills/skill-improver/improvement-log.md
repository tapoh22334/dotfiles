# skill-improver 改善履歴

## 2026-07-12 実験成果物の散逸(自己適用テスト iteration-1 で検出)
- 実物: eval-1 with_skill ランが設計した trigger クエリ集を /tmp スクラッチパッドに保存し、
  採点者が provenance を確認できず fail 判定(grading.json eval-1/with_skill)
- 分類: 脱落欠陥(スキル自身の手順7に成果物の所在規律が無かった)
- 5-why の終点: 手順5は「実験せよ」と言うが成果物をどこに残すかを指定していなかった
- 修正: 手順7に所在規律を追加(improvement-log.md 本文 or 対象スキルディレクトリ配下。
  temp は成果物でない)
- 実験: 構造スロット系につき机上照合(次回実適用時に検収)
- 回帰: 手順1-6は無変更

## 2026-07-12 テスト環境の教訓(スキル外・記録のみ)
- ベースライン(スキルなし)ランが環境登録済みの本スキルを自力発見して従い、対照が汚染された。
  次回イテレーションのベースラインは「スキル参照禁止」の明示 or 隔離環境で実施すること。
  副産物: description の自発発火性能の証拠(4/4ランが手法に到達)

## 2026-09-12 対象がスキル限定で、CLAUDE.md の古い指示による事故に発火しなかった
- 実物: ui-catarog-flood セッション 50f78709。`~/CLAUDE.md`「Backlog.md を使う。未導入なら
  backlog init」に従い `backlog init` を自動実行。運用は GitHub Issues + Project #4 に移って
  いた(github-tickets / backlog-steward は 09-07 作成)。ユーザー「なぜ自動で backlog.md が
  利用された？」→ five-whys-root-fix が発火し CLAUDE.md を直したが、分類・回帰・履歴なし。
  ユーザー「スキルだけでなく claude.md なども対象にしたい。根本原因調査に five-why を使い、
  根本原因を直すようにしたい」
- 分類: 発火欠陥(description が「EXISTING skill」限定)+ 内容欠陥(本文が SKILL.md 構造を
  前提: 発火=frontmatter、履歴=スキルディレクトリ)+ 分類表に「2 つの指示が矛盾」の欠陥種が
  無い(5-why パターンには書いてあるが直す場所が定義されていない)
- 5-why の終点: 指示アーティファクト(CLAUDE.md・スキル・memory・settings)を種類ごとに別物と
  扱い、種類をまたぐ欠陥(矛盾・陳腐化)を誰も持っていなかった。five-whys は因果を歩くが
  文面の直し方(形合わせ・回帰・履歴)を持たず、skill-improver は文面を持つが対象が狭い
- 修正(1 欠陥 = 対象の拡張。束ねず、同じ根の 4 箇所):
  1. description を「instruction artifact(skill / CLAUDE.md / memory / settings/hooks)」に
     拡張、STALE と矛盾を症状に追加、five-whys との分担を明記
  2. 「対象」節を新設: 種類 × 発火の住処 × 履歴の置き場の表、CLAUDE.md > スキルの優先順位
  3. 分類表に「矛盾欠陥」を追加(直す場所 = 古い側 + 正本を 1 か所に)
  4. 手順 3 に five-whys-root-fix との往復、手順 5 に矛盾欠陥の検収(同主題 grep)、手順 7 に
     非スキルの履歴置き場(`~/.claude/improvement-log.md`)。five-whys 側の手順 4 に
     「根本原因が指示文面なら skill-improver に渡す」の 1 文
- 実験: 発火欠陥につき机上判定(sonnet に新旧 description と 10 クエリ)。期待と 10/10 一致。
  old-A は #1,#2,#6,#8(CLAUDE.md・memory・hooks の陳腐化)に非発火、new-A は発火。
  #5(新規作成)#9(コードのバグ)は両方非発火(誤発火なし)。#7,#10 は B 先行で正しい。
  記録: 本エントリ内の要約が成果物(クエリ 10 本は上記番号の通り: 1 backlog なぜ自動 /
  2 CLAUDE.md が古い直して / 3 本スキル名指し / 4 brief が発火しない / 5 新規スキル作成 /
  6 memory が古い / 7 テストが落ちた原因 / 8 hooks が動かない / 9 関数のバグ / 10 デプロイ再発防止)
- 回帰: evals.json の 2 ケース(脱落欠陥・発火欠陥、いずれもスキル対象)は description の
  スキル向け語句を全て残しており机上で発火継続(#3,#4 で確認)。本文の手順 1〜7 の順序は不変
- 波及(未処理): Backlog.md 前提のスキル 5 本(backlog-create/list/suspend/resume、
  session-close-safe)が残り、「タスク作成」語で発火しうる。退役か GitHub 版化は別エントリ
- 同時に引く: 削除した行なし。働いていない行が観測されていないため(次の実物で判断)

## 2026-09-12 「新規作成は対象外」が、指示が存在しない根本原因を扱えなくしていた
- 実物: ユーザー「スキルがないときはスキルを作成するという手段もあり」。同日の事故でも、
  指示アーティファクトの欠陥を扱うスキル自体が無かった(不在)ことが根本原因の半分だったが、
  description の「NOT for creating new skills」が新設の選択肢を閉じていた
- 分類: 内容欠陥(分類表に「不在」が無い)+ 発火欠陥(description の除外句が不在ケースを弾く)
- 5-why の終点: 対象を「既存の」アーティファクトと定義したため、「無いこと」が欠陥として
  分類できなかった
- 修正: 分類表に「不在欠陥」を追加(新設する種類を観測で決める条件文: 2 回以上+複数段 →
  新スキル / 1〜2 文 → CLAUDE.md / 本人固有 → memory。近いスキルがあれば拡張)。description の
  除外句を「不在が根本原因なら新設は正当な修正。分類はここ、起草は writing-skills」に置換。
  writing-skills との関係節に受け渡しと記録の置き場を追記
- 実験: 省略(分類表への行追加と除外句の置換で、机上では前エントリの 10 クエリの判定に変化なし。
  次に不在欠陥の実物が出たときに検収)
- 回帰: 「NOT for creating new skills」を外したことで #5(新規スキルを作りたい)が誤発火しないか
  を机上確認: 新文面は「root cause が不在のとき」に限定しており、単純な作成依頼は
  writing-skills / skill-creator 側の語句で先に拾われる。要観測

## 2026-09-13 不在欠陥の初適用: 構造化の規則を持つスキルが無かった → structuring-work を新設
- 実物: github-tickets に検査表を足した直後、ユーザーが「登録とは別に構造化スキルを経由すべき」と
  指摘。近いスキル(github-tickets)への拡張で済ませたが、対象の性質(トラッカー非依存・知識が重い)
  から独立が正しかった
- 分類: 不在欠陥。ただし直前の判断「近いスキルがあれば拡張」が誤り側に倒れた例
- 5-why の終点: 「近いスキルがあれば拡張」の条件に「知識の重さ」「トラッカー非依存か」が無く、
  機構と知識の同居を許した
- 修正(本スキル): 不在欠陥の行に条件を 1 つ追加 —「近いスキルが**機構**(登録・実行)で、欠けて
  いるのが**知識・判断規則**なら、拡張ではなく新設して機構側から参照させる」
- 実験: 省略(条件文の追加。次の不在欠陥で検収)
- 回帰: 前 2 エントリの判定(#1〜#10 の机上)は不変
